import React from 'react'
import { BrowserRouter, Routes, Route } from 'react-dom-router'

const Router = () => {
    return(
        <div>
            Router
        </div>
    )
}

export default Router;